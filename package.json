{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --scope={@walnut/common,@walnut/server}",
    "new-version": "lerna version --conventional-commits --create-release github",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "name": "yarn-workspaces-tutorial"
}
